#
# DATABASE CONFIGURATION
#
# Default is Mysql. Change to use a different one.
DATABASE_TYPE=mysql
#DATABASE_TYPE=psql
#DATABASE_TYPE=oracle
#DATABASE_TYPE=sqlite
#
# MYSQL Config (used for Mysql and RaceDB containers)
# The MYSQL_PASSWORD is also set the root password
MYSQL_USER=racedb
MYSQL_PASSWORD=[fill me in]
MYSQL_DATABASE=racedb
# Used when MYSQL starts up
MYSQL_ROOT_PASSWORD=[fill me in]
# Do not fill this in unless an external MYSQL is used
# MYSQL_HOST=[databasehostip]
#
# Postgresql Config (from postgres container)
#POSTGRES_USER=racedb
#POSTGRES_PASSWORD=[fill me in]
#POSTGRES_DB=racedb
